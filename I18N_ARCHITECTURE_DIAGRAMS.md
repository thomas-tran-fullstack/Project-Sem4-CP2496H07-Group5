# EZMart i18n Architecture Diagram & Flow

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    EZMart Application                           │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              Browser / Client Layer                    │    │
│  │  ┌──────────────────────────────────────────────────┐  │    │
│  │  │  header.xhtml (Language Selector UI)            │  │    │
│  │  │                                                  │  │    │
│  │  │  [🌐 Language] ▼                                │  │    │
│  │  │  ┌─────────────────────────────────────────┐   │  │    │
│  │  │  │ English              (Default Selected) │   │  │    │
│  │  │  │ Tiếng Việt                             │   │  │    │
│  │  │  │ Español                                │   │  │    │
│  │  │  │ Deutsch                                │   │  │    │
│  │  │  │ 日本語                                  │   │  │    │
│  │  │  │ Français                               │   │  │    │
│  │  │  │ Português                              │   │  │    │
│  │  │  │ ... (18 more languages)                │   │  │    │
│  │  │  └─────────────────────────────────────────┘   │  │    │
│  │  └──────────────────────────────────────────────────┘  │    │
│  └────────────────────────────────────────────────────────┘    │
│                          ↓                                       │
│  ┌────────────────────────────────────────────────────────┐    │
│  │           JSF Request Processing                       │    │
│  │  ┌──────────────────────────────────────────────────┐  │    │
│  │  │  User clicks language: #{locale.setCurrentLanguage('vi')}
│  │  │  Request sent to server                        │  │    │
│  │  └──────────────────────────────────────────────────┘  │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                          ↓
        ┌─────────────────────────────────────┐
        │    GlassFish Application Server     │
        │                                     │
        │  Jakarta EE 10 / JSF 4.0 / CDI     │
        └─────────────────────────────────────┘
                          ↓
        ┌─────────────────────────────────────────────────┐
        │         LocaleController (Session Scoped)       │
        │                                                 │
        │  @Named("locale")                              │
        │  @SessionScoped                                │
        │                                                 │
        │  - currentLanguage: String = "en"              │
        │                                                 │
        │  Methods:                                       │
        │  ├─ getCurrentLanguage() → "en"                │
        │  ├─ setCurrentLanguage(lang) → update locale   │
        │  ├─ getMessage(key) → fetch translation        │
        │  ├─ getLanguageName(code) → "Español"         │
        │  ├─ getLanguages() → ["en", "vi", ...]        │
        │  └─ getCurrentLocale() → Locale(lang)          │
        │                                                 │
        └─────────────────────────────────────────────────┘
                   ↓                        ↓
        ┌──────────────────┐    ┌─────────────────────┐
        │ FacesContext     │    │ Session Map         │
        │                  │    │                     │
        │ locale = new     │    │ "appLocale" →       │
        │ Locale("vi")     │    │ Locale("vi")        │
        │                  │    │                     │
        └──────────────────┘    └─────────────────────┘
                   ↓                        ↓
        ┌──────────────────────────────────────────┐
        │   ResourceBundle Manager                 │
        │                                          │
        │  ResourceBundle.getBundle(               │
        │    "messages.messages",                  │
        │    new Locale("vi")                      │
        │  )                                       │
        │                                          │
        │  Returns Bundle: messages_vi.properties  │
        └──────────────────────────────────────────┘
                          ↓
        ┌──────────────────────────────────────────┐
        │  Message Resource Files (Classpath)      │
        │                                          │
        │  EJB Module JAR                          │
        │  └─ messages/                            │
        │     ├─ messages_en.properties            │
        │     ├─ messages_vi.properties (← LOADED) │
        │     ├─ messages_es.properties            │
        │     ├─ messages_de.properties            │
        │     ├─ messages_ja.properties            │
        │     ├─ ... (18 more languages)           │
        │     └─ messages_zh.properties            │
        │                                          │
        │  Each file contains 60+ key-value pairs  │
        │  Example from messages_vi.properties:   │
        │  ┌─────────────────────────────────┐    │
        │  │ login.title=Đăng nhập           │    │
        │  │ login.subtitle=Chào mừng...     │    │
        │  │ error.emailNotFound=Không tìm.. │    │
        │  │ success.loginSuccess=Đăng nhập..│    │
        │  └─────────────────────────────────┘    │
        │                                          │
        └──────────────────────────────────────────┘
                          ↓
        ┌──────────────────────────────────────────┐
        │   Message Retrieval                      │
        │                                          │
        │  locale.getMessage("login.title")        │
        │        ↓                                 │
        │  bundle.getString("login.title")         │
        │        ↓                                 │
        │  Returns: "Đăng nhập"                    │
        │                                          │
        └──────────────────────────────────────────┘
                          ↓
        ┌──────────────────────────────────────────┐
        │   JSF Page Rendering                     │
        │                                          │
        │  #{locale.getMessage('login.title')}     │
        │        ↓ renders as ↓                    │
        │   Đăng nhập                              │
        │                                          │
        └──────────────────────────────────────────┘
                          ↓
        ┌──────────────────────────────────────────┐
        │   HTML Response to Browser                │
        │                                          │
        │  <h1>Đăng nhập</h1>                      │
        │  Page updated with Vietnamese text       │
        │                                          │
        └──────────────────────────────────────────┘
```

---

## Request/Response Flow Diagram

```
User Interaction Timeline:

T0: Page Load
    Browser GET /app/login
    ↓
    Server renders login.xhtml
    LocaleController loaded from session (default: en)
    ResourceBundle loads messages_en.properties
    HTML rendered in English
    ↓
    Response: HTML with English text

T1: User Clicks Language Selector
    Browser: User clicks "Tiếng Việt" button
    ↓
    JSF Form submits to #{locale.setCurrentLanguage('vi')}
    ↓
    Server:
    1. LocaleController.setCurrentLanguage('vi') called
    2. currentLanguage field updated to "vi"
    3. FacesContext.getViewRoot().setLocale(new Locale("vi"))
    4. Session map updated: put("appLocale", new Locale("vi"))
    ↓
    Page re-renders with:
    LocaleController now uses Locale("vi")
    ResourceBundle loads messages_vi.properties
    All getMessage() calls return Vietnamese strings
    ↓
    Response: HTML with Vietnamese text

T2+: Subsequent Navigation
    User navigates to other pages
    Browser GET /app/register (same session)
    ↓
    Server:
    1. LocaleController retrieved from HTTP session
    2. currentLanguage = "vi" (persisted from previous step)
    3. ResourceBundle loads messages_vi.properties
    4. register.xhtml rendered in Vietnamese
    ↓
    Response: HTML with Vietnamese text
    (Language persists across navigation)

T3: Session Expires
    HTTP Session timeout triggered
    LocaleController discarded
    ↓
    New request creates new session
    New LocaleController instantiated with default language "en"
    ↓
    Response: HTML with English text
```

---

## Data Flow: Message Translation

```
Input: User wants translation of key "error.emailNotFound"

Step 1: Access in XHTML
        ┌──────────────────────────────────────┐
        │ #{locale.getMessage('error.emailNotFound')} │
        └──────────────────────────────────────┘

Step 2: Java Method Call
        ┌──────────────────────────────────────┐
        │ LocaleController.getMessage(key)     │
        │ ├─ Input: "error.emailNotFound"      │
        │ └─ Lookup: currentLanguage = "vi"    │
        └──────────────────────────────────────┘

Step 3: ResourceBundle Load
        ┌──────────────────────────────────────┐
        │ ResourceBundle.getBundle(             │
        │   "messages.messages",                │
        │   new Locale("vi")                    │
        │ )                                     │
        │ ↓ Java looks for:                    │
        │   messages_vi.properties              │
        │   (falls back to messages.properties) │
        └──────────────────────────────────────┘

Step 4: Key Lookup
        ┌────────────────────────────────────────┐
        │ messages_vi.properties                 │
        │                                        │
        │ login.title=Đăng nhập vào tài khoản   │
        │ ...                                    │
        │ error.emailNotFound=Không tìm thấy ... │ ← FOUND
        │ ...                                    │
        └────────────────────────────────────────┘

Step 5: Return Translation
        ┌────────────────────────────────────────┐
        │ bundle.getString("error.emailNotFound") │
        │ ↓ returns:                             │
        │ "Không tìm thấy tài khoản với email ... │
        └────────────────────────────────────────┘

Output: Translated text
        ┌────────────────────────────────────────┐
        │ "Không tìm thấy tài khoản với email ... │
        │                                        │
        │ Rendered in browser as error message   │
        └────────────────────────────────────────┘
```

---

## File Organization & Deployment

```
Development Environment:
┌─────────────────────────────────────────────────────────┐
│ Project Root (c:\Users\Admin\Documents\GitHub\...)      │
│                                                         │
│ EZMart_Supermarket_Management-ejb/                      │
│ ├─ src/java/messages/          ← SOURCE (23 files)     │
│ │  ├─ messages_en.properties                           │
│ │  ├─ messages_vi.properties                           │
│ │  ├─ messages_es.properties                           │
│ │  ├─ messages_de.properties                           │
│ │  ├─ messages_ja.properties                           │
│ │  ├─ ... (18 more language files)                     │
│ │  └─ messages_zh.properties                           │
│ │                                                      │
│ └─ src/java/sessionbeans/                              │
│    └─ LocaleController.java                            │
│                                                         │
│ EZMart_Supermarket_Management-war/                      │
│ └─ web/templates/user/                                 │
│    └─ header.xhtml (contains language selector)        │
│                                                         │
└─────────────────────────────────────────────────────────┘

          ↓ Compile & Package ↓

Deployed on GlassFish 7.0.15:
┌─────────────────────────────────────────────────────────┐
│ GlassFish Domain                                        │
│                                                         │
│ applications/EZMart_EAR.ear/                            │
│ ├─ EZMart-ejb.jar                                       │
│ │  └─ messages/                  ← RUNTIME (23 files)  │
│ │     ├─ messages_en.properties                        │
│ │     ├─ messages_vi.properties                        │
│ │     ├─ messages_es.properties                        │
│ │     ├─ ... (20 more language files)                  │
│ │     └─ messages_zh.properties                        │
│ │                                                      │
│ └─ EZMart-war.war/                                      │
│    ├─ templates/user/header.xhtml                      │
│    ├─ ... (other JSF pages)                            │
│    └─ WEB-INF/                                         │
│                                                         │
└─────────────────────────────────────────────────────────┘

ClassLoader Classpath:
┌─────────────────────────────────────────────────────────┐
│ When LocaleController calls:                            │
│ ResourceBundle.getBundle("messages.messages", ...)      │
│                                                         │
│ Java looks in classpath:                               │
│ ├─ EZMart-ejb.jar                                       │
│ │  └─ messages/messages_XX.properties ← FOUND         │
│ ├─ application/lib/                                     │
│ ├─ GlassFish/lib/                                       │
│ └─ JDK/lib/                                             │
│                                                         │
│ Loads and caches messages_XX.properties                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## Language Selection State Diagram

```
┌─────────────────────────────────────────────────────────┐
│               Application Lifecycle                      │
└─────────────────────────────────────────────────────────┘

User Access Application
  │
  ├─ New User (No Session)
  │   └─ LocaleController instantiated
  │       └─ currentLanguage = "en" (default)
  │           └─ Browser renders in English
  │
  └─ Returning User (Session Exists)
      └─ LocaleController retrieved from session
          └─ currentLanguage = previously selected value
              └─ Browser renders in that language

              ↓ After language selection ↓

User Clicks: "Español"
  │
  └─ LocaleController.setCurrentLanguage("es")
      │
      ├─ currentLanguage field = "es"
      │
      ├─ FacesContext update
      │   └─ viewRoot.setLocale(new Locale("es"))
      │
      ├─ Session persistence
      │   └─ sessionMap.put("appLocale", new Locale("es"))
      │
      └─ ResourceBundle reload
          └─ getBundle("messages.messages", Locale("es"))
              └─ Load messages_es.properties

              ↓ Page refreshes ↓

JSF View Re-render
  │
  ├─ #{locale.getMessage('key')} calls
  │   ├─ Use current language = "es"
  │   └─ Fetch from messages_es.properties
  │       └─ Return Spanish translation
  │
  └─ HTML Response with Spanish text

              ↓ User navigates ↓

Different Page Request (Same Session)
  │
  └─ LocaleController retrieved from session
      └─ currentLanguage still = "es"
          └─ Page renders in Spanish
              └─ Language persists across navigation

              ↓ Session timeout ↓

Session Expires
  │
  └─ LocaleController discarded
      └─ New session created
          └─ New LocaleController
              └─ currentLanguage = "en" (default)
                  └─ Back to English (unless user logs in again)
```

---

## Key-Value Pair Resolution Flow

```
Example: User selects Vietnamese, clicks login page

┌──────────────────────────────────────┐
│ JSF Page (login.xhtml)               │
├──────────────────────────────────────┤
│ <h1>#{locale.getMessage('login.title')}</h1>
└──────────────────────────────────────┘
                    ↓
┌──────────────────────────────────────┐
│ LocaleController Bean                │
├──────────────────────────────────────┤
│ currentLanguage = "vi"               │
│                                      │
│ public String getMessage("login.title")
│  └─ Create ResourceBundle            │
└──────────────────────────────────────┘
                    ↓
┌──────────────────────────────────────┐
│ ResourceBundle Factory               │
├──────────────────────────────────────┤
│ getBundle("messages.messages",       │
│           new Locale("vi"))          │
│                                      │
│ Searches for:                        │
│ 1. messages_vi.properties ✓ FOUND    │
│ 2. messages.properties (fallback)    │
└──────────────────────────────────────┘
                    ↓
┌──────────────────────────────────────┐
│ messages_vi.properties               │
├──────────────────────────────────────┤
│ login.title=Đăng nhập tài khoản      │
│ login.subtitle=Chào mừng đến...      │
│ login.emailLabel=Địa chỉ email       │
│ ...                                  │
│ (60+ more key-value pairs)           │
└──────────────────────────────────────┘
                    ↓
┌──────────────────────────────────────┐
│ Message Lookup                       │
├──────────────────────────────────────┤
│ bundle.getString("login.title")      │
│                                      │
│ Returns: "Đăng nhập tài khoản"      │
└──────────────────────────────────────┘
                    ↓
┌──────────────────────────────────────┐
│ HTML Rendering                       │
├──────────────────────────────────────┤
│ <h1>Đăng nhập tài khoản</h1>        │
└──────────────────────────────────────┘
                    ↓
        ┌────────────────────────┐
        │  Browser Display:      │
        │                        │
        │  ┌──────────────────┐  │
        │  │ Đăng nhập tài    │  │
        │  │ khoản            │  │
        │  └──────────────────┘  │
        └────────────────────────┘
```

---

## Cache and Performance

```
First Request (Language Selection):
┌─────────────────────────────────────────────────────────┐
│ ResourceBundle.getBundle() called                      │
│ ├─ Classpath scan for messages_vi.properties           │
│ ├─ File located in EJB JAR                             │
│ ├─ File contents read and parsed                       │
│ ├─ HashMap created with 60+ key-value pairs            │
│ ├─ JVM caches the Bundle                               │
│ └─ Returns cached instance                             │
│                                                         │
│ Performance Impact: ~10-50ms (first load)               │
└─────────────────────────────────────────────────────────┘

Subsequent Requests (Same Language):
┌─────────────────────────────────────────────────────────┐
│ ResourceBundle.getBundle() called                      │
│ └─ JVM returns cached Bundle instance                  │
│    └─ getMessage() does HashMap lookup                 │
│       └─ O(1) hash table access                        │
│                                                         │
│ Performance Impact: <1ms (cached)                       │
└─────────────────────────────────────────────────────────┘

Memory Profile:
┌─────────────────────────────────────────────────────────┐
│ Per Language (Cached):                                  │
│ messages_XX.properties:  ~50 KB                         │
│                                                         │
│ All 24 Languages (Worst Case):                          │
│ 24 * 50 KB = 1.2 MB                                     │
│                                                         │
│ Session Per User:                                       │
│ String (language code) = ~32 bytes                      │
│ Locale object = ~64 bytes                               │
│ Total per session = ~96 bytes                           │
│                                                         │
│ 1000 concurrent users = ~96 KB session overhead         │
└─────────────────────────────────────────────────────────┘
```

---

## Security Flow

```
┌─────────────────────────────────────────────────────────┐
│ Security Layers                                         │
└─────────────────────────────────────────────────────────┘

Layer 1: Input Validation
┌─────────────────────────────────────────────────────────┐
│ setCurrentLanguage(lang)                                │
│ ├─ Validate language code is in SUPPORTED_LANGUAGES    │
│ ├─ Reject invalid codes (e.g., "'; DROP TABLE users")  │
│ └─ Only accept: en, vi, es, de, ja, fr, pt, ru, ko... │
└─────────────────────────────────────────────────────────┘

Layer 2: Session Isolation
┌─────────────────────────────────────────────────────────┐
│ Language preference stored in HTTP Session             │
│ ├─ Each user has separate session                      │
│ ├─ Cannot access other user's language choice          │
│ └─ Session timeout removes all data                    │
└─────────────────────────────────────────────────────────┘

Layer 3: Message Content Security
┌─────────────────────────────────────────────────────────┐
│ getMessage(key)                                         │
│ ├─ Key is fixed string (not from user input)           │
│ ├─ ResourceBundle loads from classpath (trusted)       │
│ ├─ No dynamic SQL or code execution                    │
│ └─ HTML escaping automatic via JSF                    │
└─────────────────────────────────────────────────────────┘

Layer 4: Classpath Integrity
┌─────────────────────────────────────────────────────────┐
│ Resource files are immutable:                           │
│ ├─ Bundled in application JAR at build time            │
│ ├─ Cannot be modified at runtime                       │
│ ├─ Signed JAR (optional) provides integrity            │
│ └─ No file system access needed                        │
└─────────────────────────────────────────────────────────┘

Result: No injection, XSS, or data leakage vulnerabilities
```

---

## Scalability Considerations

```
Linear Scalability with Language Count:
┌─────────────────────────────────────────┐
│ Number of   | File    | Cache   | Lookup│
│ Languages   | Size    | Memory  | Time  │
├─────────────┼─────────┼─────────┼───────┤
│ 4           | ~200KB  | ~200KB  | O(1)  │
│ 8           | ~400KB  | ~400KB  | O(1)  │
│ 16          | ~800KB  | ~800KB  | O(1)  │
│ 24          | ~1.2MB  | ~1.2MB  | O(1)  │
│ 50          | ~2.5MB  | ~2.5MB  | O(1)  │
│ 100         | ~5MB    | ~5MB    | O(1)  │
└─────────────┴─────────┴─────────┴───────┘

Lookup performance is O(1) regardless of language count!
Adding more languages doesn't slow down message retrieval.

Session Memory with User Count:
┌──────────────────────────────────────────┐
│ Active Users | Session Memory (approx)   │
├──────────────┼──────────────────────────┤
│ 100          | ~10 KB                    │
│ 1,000        | ~100 KB                   │
│ 10,000       | ~1 MB                     │
│ 100,000      | ~10 MB                    │
└──────────────┴──────────────────────────┘

Language persistence adds minimal memory overhead
to each user session (~32-96 bytes per user).
```

---

This completes the comprehensive i18n architecture documentation!
